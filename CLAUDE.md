# CLAUDE.md

このファイルは Claude Code がこのリポジトリで作業する際のコンテキストです。

## プロジェクト概要

セッション登壇者向けの、ブラウザ全画面表示用カウントダウンタイマー。
ビルドプロセスを持たない **単一HTMLファイル（`index.html`）** で完結する。
フレームワーク・外部ライブラリ・依存パッケージは一切使用しない方針。

## 技術方針

- Vanilla HTML / CSS / JavaScript のみ。フレームワーク導入は行わない。
- `index.html` 一つに HTML / CSS / JS をインライン記述する（現状の構成を維持）。
- 外部リソース（CDN、Webフォント等）への依存を持たせない。オフラインでも単体で動作すること。
- 対応ブラウザは最新の Chrome / Edge を主対象（登壇PCでの利用を想定）。

## 主要機能（変更時は仕様を維持すること）

- 設定画面：分・秒の入力、背景色・文字色のカラーピッカー
- 開始ボタンは「全画面表示」「ウィンドウ表示」の2種類を用意する
  - 「全画面表示」はユーザー操作起点の Fullscreen API を呼び出す
    （ブラウザ仕様上、自動全画面化はできないため必ずボタン操作を挟む）
  - 「ウィンドウ表示」は全画面化せず（既に全画面中なら解除して）そのままタイマー画面を表示する
- カウントダウン表示は `MM:SS`、フォントは Arial Bold、画面幅に応じて可変（`vw`指定）
- 持ち時間終了後は自動でカウントアップに切り替え、背景を赤 `#e60000` と黒で1秒間隔で点滅させる
- 一時停止 / 時間をリセット（設定値の持ち時間に巻き戻す） / 設定に戻る操作は画面下部にホバー時のみ表示（登壇中の視界を妨げない）
- 「設定に戻る」操作は全画面表示を解除しない（全画面のまま設定画面に遷移し、開始ボタンで再度タイマー画面に戻る）
- キーボードショートカット：`Space`＝一時停止/再開、`R`＝設定に戻る

## デプロイ

- `main` ブランチへの push で `.github/workflows/deploy.yml` から GitHub Pages に自動デプロイされる。
- 静的ファイルのみのため、ビルドステップは無し（`actions/upload-pages-artifact` でリポジトリ直下をそのまま公開）。

## 作業時の注意

- 依存追加（npmパッケージ等）が必要になった場合は、まずこの方針（依存ゼロ）からの逸脱として
  ユーザーに確認を取ってから進めること。
- 見た目・配色・レイアウトの仕様変更は、上記「主要機能」の記述と齟齬が出ないよう、
  変更後にこのファイルも更新すること。
